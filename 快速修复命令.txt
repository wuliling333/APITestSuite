# 快速修复依赖问题 - 复制以下命令到终端运行

# 步骤1：安装所有依赖（推荐，一次性安装所有需要的包）
cd /Users/rourou/Desktop/APITestSuite
source .venv/bin/activate
python3 -m pip install --no-cache-dir pyyaml requests jinja2 pytest pytest-html openpyxl protobuf

# 如果步骤1报权限错误，尝试步骤2：逐个安装
cd /Users/rourou/Desktop/APITestSuite
source .venv/bin/activate
python3 -m pip install --no-cache-dir pyyaml
python3 -m pip install --no-cache-dir requests
python3 -m pip install --no-cache-dir jinja2
python3 -m pip install --no-cache-dir pytest
python3 -m pip install --no-cache-dir pytest-html
python3 -m pip install --no-cache-dir openpyxl
python3 -m pip install --no-cache-dir protobuf

# 验证安装
python3 -c "import yaml; print('✓ yaml 安装成功')"
python3 -c "import requests; print('✓ requests 安装成功')"
python3 -c "import jinja2; print('✓ jinja2 安装成功')"

# 然后就可以运行了
python3 main.py --run

